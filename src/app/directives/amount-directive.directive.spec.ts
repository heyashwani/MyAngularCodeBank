import { AmountDirectiveDirective } from './amount-directive.directive';

describe('AmountDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
